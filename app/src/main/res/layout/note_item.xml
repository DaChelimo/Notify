<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:card_view="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
                name="noteItem"
                type="com.ramadan.notify.data.model.WrittenNote" />

        <variable
                name="whiteboardItem"
                type="com.ramadan.notify.data.model.Whiteboard" />
    </data>

    <androidx.cardview.widget.CardView
            android:id="@+id/note"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="6dp"
            android:background="@{noteItem.noteColor}"
            android:clickable="true"
            android:focusable="true"
            android:minHeight="120dp"
            card_view:cardCornerRadius="14dp">

        <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:padding="2dp">

            <TextView
                    android:id="@+id/name"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="1dp"
                    android:layoutDirection="inherit"
                    android:text="@{noteItem.name}"
                    android:textAlignment="inherit"
                    android:textColor="@color/colorPrimary"
                    android:textSize="10sp"
                    android:textStyle="bold" />

            <com.ramadan.notify.utils.LinedTextView
                    android:id="@+id/content"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@{noteItem.content}"
                    android:textAlignment="inherit"
                    android:textColor="@color/colorPrimary"
                    android:textDirection="inherit"
                    android:textSize="14sp" />

            <ImageView
                    android:id="@+id/whiteboard"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:src="@{whiteboardItem.path}" />

        </LinearLayout>


    </androidx.cardview.widget.CardView>
</layout>
